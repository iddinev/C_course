#include <stdio.h>

#include "hangman_functions.c"


// TODO:
// Select & display a random word from the file
//  if not in the list of already used - add it.

int main()
{
    /* mainMenu(); */
    while (!mainMenu()) { }

    return 0;
}
